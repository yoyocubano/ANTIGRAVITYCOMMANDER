name = "antigravity-commander"
main = "dashboard_server.py"
compatibility_date = "2024-01-14"

[site]
bucket = "./templates"

[vars]
ENVIRONMENT = "production"

# Configuración del Túnel (Opcional, si usas Cloudflare Tunnel gestionado localmente)
# tunnel_id = "..."
# credentials_file = "..."

[observability]
enabled = true
head_sampling_rate = 1
